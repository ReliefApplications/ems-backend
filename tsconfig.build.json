{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./build"
  },
  "exclude": [
    "node_modules",
    "build",
    // there's a wrong link in this file, needed for migration
    "src/utils/migrations/template.ts",
    // exclude test files
    "jest.config.ts",
    "__tests__/**/*"
  ]
}
