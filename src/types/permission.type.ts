/**
 * Define the enum for ResourcePermission
 */
export enum resourcePermission {
  CREATE_RECORDS = 'canCreateRecords',
  SEE_RECORDS = 'canSeeRecords',
  UPDATE_RECORDS = 'canUpdateRecords',
  DELETE_RECORDS = 'canDeleteRecords',
  DOWNLOAD_RECORDS = 'canDownloadRecords',
}
