/* eslint-disable @typescript-eslint/naming-convention */
/** Enum definition for placeholder text in emails */
export enum EmailPlaceholder {
  TODAY = '{{today}}',
  DATASET = '{{dataset}}',
  NOW = '{{now}}',
}
